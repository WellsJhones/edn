print("Let's calculate a box volume")

height = int(input("Type the height of the box: \n"))
width = int(input("Type now the width of the box: \n"))
length = int(input("Type at last the length of the box: \n"))

print("The cubic value is: " + str(width * length * height))
